# Modig

Modig is a mobile application that allows the user to find interesting places nearby. The user selects the location and category, the application uses the user's location and provides it with interesting places
